import { NextResponse } from 'next/server';\n\nexport async function GET(request, { params }) {\n  const { id } = params;\n  return NextResponse.json({ message: `Resource 3 details for ID: ${id}` });\n} \n\nexport async function PUT(request, { params }) {\n  const { id } = params;\n  const body = await request.json();\n  return NextResponse.json({ message: `Resource 3 ${id} updated!` });\n}\n\nexport async function DELETE(request, { params }) {\n  const { id } = params;\n  return NextResponse.json({ message: `Resource 3 ${id} deleted!` });\n}