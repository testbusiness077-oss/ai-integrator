import { NextResponse } from 'next/server';\n\nexport async function GET(request, { params }) {\n  const { id } = params;\n  // Logic to fetch a single resource by id\n  return NextResponse.json({ message: `Fetching resource with ID: ${id}` });\n} \n\nexport async function PUT(request, { params }) {\n  const { id } = params;\n  const body = await request.json();\n  // Logic to update resource with id\n  return NextResponse.json({ message: `Resource ${id} updated!` });\n}\n\nexport async function DELETE(request, { params }) {\n  const { id } = params;\n  // Logic to delete resource with id\n  return NextResponse.json({ message: `Resource ${id} deleted!` });\n}