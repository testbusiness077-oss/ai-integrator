import React from 'react';\n\ntype InputProps = {\n  register: any;\n  name: string;\n  error?: string;\n  type?: string;\n  placeholder?: string;\n};\n\nconst Input: React.FC<InputProps> = ({ register, name, error, type = 'text', placeholder }) => {\n  return (\n    <div className='flex flex-col mb-4'>\n      <input {...register(name)} type={type} placeholder={placeholder} className={`border rounded p-2 ${error ? 'border-red-500' : 'border-gray-300'}`} />\n      {error && <p className='text-red-500'>{error}</p>}\n    </div>\n  );\n};\n\nexport default Input;